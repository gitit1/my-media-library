// Theme:
export enum Theme {
	Light = 'light',
	Dark = 'dark',
}

// Button:
export enum BtnSize {
	Small = 'sm',
	Medium = 'md',
	Large = 'lg',
}

export enum BtnType {
	Button = 'button',
	Submit = 'submit',
	Reset = 'reset',
}

export enum BtnVariant {
	Default = 'default',
	Outline = 'outline',
}

// Card:
export enum CardsView {
	Grid = 'grid',
	List = 'list',
	Table = 'table',
}

// Typography:
export enum TypographyType {
	H1 = 'h1',
	H2 = 'h2',
	H3 = 'h3',
	H4 = 'h4',
	H5 = 'h5',
	H6 = 'h6',
	P = 'p',
	Span = 'span',
}

// Flex:
export enum FlexAlign {
	Center = 'center',
	Start = 'start',
	End = 'end',
}

export enum FlexJustify {
	Between = 'between',
	Center = 'center',
	Start = 'start',
	End = 'end',
}

export enum FlexDirection {
	Row = 'row',
	Col = 'col',
}
